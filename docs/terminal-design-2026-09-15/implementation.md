# Terminal implementation

Implemented September 15, 2026 in `site-v2` across all 18 generated routes.

## Delivered

- Persistent top-menu Studio / Terminal switch, applied before first paint and through Astro navigation.
- Dark editor shell, cyan highlights, real explorer links, document breadcrumb, operational rail and responsive status bar.
- Alternate homepage delivery diagram, compact Work rows with distinct project schematics, career outline, case-study contents navigation, login access flow and themed Lab/account/error pages.
- Recursive monospace chrome and Onest reading text; case-study prose limited to 68 characters.
- Decorative 1100ms cursor blink, one-time connector reveal and restrained control transitions; reduced-motion and hidden-document handling.
- Stable tool component identity preserves analyzer drafts/results, graph selection/camera/conversation and unsaved settings across switches.

## Validation

Production build: 18 pages, zero Astro errors, warnings or hints.

Playwright coverage includes all routes at 320, 390, 768, 1280 and 1536 pixels, original Studio behavior, persistence, storage failure, reduced motion, tool-state retention, signed-out access, loaded classifier, entity records and expanded graph desktop/mobile controls. Auth and execution services are mocked; no live account or quota was consumed. Screenshots are generated in `site-v2/test-results` and are ephemeral test output.

The independent Impeccable finish review identified four material fixes, all addressed: distinct project schematics, hydrated tool captures, prose reading width and the nonexistent auth breadcrumb ancestor.

## Reference interpretation

The PNGs remain concept images. Actual source copy, available controls, permissions and graph data govern implementation. The real graph has 82 entities, classifier values come from its existing simulation, and long pages scroll naturally. Illustrative preview schematics are labeled. Studio remains the initial default; choose Terminal in the top menu to view the alternate version.

Implementation is local and has not been deployed.
